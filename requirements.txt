# Content Pipeline Dependencies
# Install: pip install -r requirements.txt

# Custom MCP server (X/Twitter + YouTube posting)
mcp[cli]>=1.0.0
python-dotenv>=1.0.0

# X/Twitter API v2 client
tweepy>=4.14.0

# YouTube Data API v3
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0

# CI/CD script — Reddit posting (MCP server uses npx, script needs direct client)
praw>=7.7.0

# Feed curation — blog roll / RSS / newsletter ingestion
feedparser>=6.0.0
trafilatura>=1.6.0
readability-lxml>=0.8.1

# Visual rendering — infographic-style layouts with pixel-precise text placement
Pillow>=10.0.0

# HTML/CSS visuals + programmatic hero backdrops rendered via headless Chromium
# After install run: python -m playwright install chromium
playwright>=1.40.0

# AI image generation — OPTIONAL photoreal/illustrative path (image_generation mode = ai).
# The default mode is `programmatic` (deterministic, free, offline) and does NOT need this.
# Works for both OpenAI and Azure OpenAI image/vision models via one SDK.
openai>=1.40.0
