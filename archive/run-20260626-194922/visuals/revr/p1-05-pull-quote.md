# REVR — p1-05-pull-quote.png

- Source artifact: `content/from-prompts-to-loop-engineering.md` (the reframe)
- Renderer: `content/visuals/render_loop_engineering.py` :: `pull_quote()` (typographic)
- Model (blind read): GPT (multimodal) in GitHub Copilot

## SOURCE INTENT

A typographic reframe: "You're not getting better at prompting. The level you work at is moving up the
stack." Supporting line: prompt → context → rig → loop, each step automates the craft below.

## Iteration 1 — PASS

DERIVED READ (blind):
- One-line message: "THE REFRAME — 'You're not getting better at prompting. The level you work at is
  moving up the stack.' with a subline 'prompt → context → rig → loop: each step automates the craft
  below it.'"
- Element inventory: eyebrow, headline quote (one accent word emphasized), subline, footer attribution.
  Pure typography — no data channels to decode, nothing `UNDECODABLE`.
- Color/shape semantics: single accent highlight on "moving"; no encoded categories.

SCORE: 96/100 (message 30 / encoding 25 / completeness 15 / order 14 / no-false-signal 10 / standalone 2)

VERDICT: **PASS** — score ≥ 85, no encoding to mis-read, blind message matches source intent.
