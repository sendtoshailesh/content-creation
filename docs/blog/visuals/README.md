# Blog Visuals

This directory contains visual assets (PNG images) for published blog posts. Files here are copied from `content/visuals/` by the `@web-publisher` agent during the publishing step.

## Contents

| File | Blog Post |
|------|-----------|
| `before-after-metrics.png` | PostgreSQL EXPLAIN BUFFERS case study |
| `buffer-hit-comparison.png` | PostgreSQL EXPLAIN BUFFERS case study |
| `pg-version-timeline.png` | PostgreSQL EXPLAIN BUFFERS case study |
| `query-plan-tree.png` | PostgreSQL EXPLAIN BUFFERS case study |
| `shared-buffers-flow.png` | PostgreSQL EXPLAIN BUFFERS case study |

## Usage

Images are referenced by blog HTML pages using relative paths (e.g., `visuals/filename.png`). Do not rename or move files once they are referenced by a published post.
